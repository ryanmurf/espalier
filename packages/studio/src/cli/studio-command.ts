export interface StudioCommandOptions {
  port?: number;
  host?: string;
  writeMode?: boolean;
  config?: string;
}

export function runStudioCommand(_options: StudioCommandOptions): void {
  // Placeholder — will launch StudioServer
}
